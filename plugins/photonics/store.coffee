Photonics = require('./core.coffee')

class Photonics.Store
  constructor: (@serverAddress) ->
  insert: (options) ->
  update: (id, options) ->
  remove: (id) ->
  find: (options) ->
  findOne: (options) ->
  customFind: (name, options) ->

module.exports = Photonics